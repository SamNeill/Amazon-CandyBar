rootProject.name = "candybar"

gradle.ext.isJitPack = System.getenv('JITPACK') != null

include ':extLibs:PreLollipopTransitions'
include ':library'

if (!gradle.ext.isJitPack) {
    include ':app'
}
